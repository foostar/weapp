<view class="my-header">
    <view wx:if="{{!isLogin}}">
        <image src="{{}}"></image>
        <text>请点击登录／注册</text>
    </view>
    <view wx:if="{{isLogin}}">
        <image src="{{}}"></image>
        <text>登录／注册</text>
    </view>
    
</view>
