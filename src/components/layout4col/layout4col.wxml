<view class="layout4col layout-row">
    <view class="layout-4-col-item {{style}} {{item.iconStyle}}" wx:for="{{componentList}}" bindtap="layout4col_touch" data-index="{{ index }}" data-event-key="{{key}}" wx:key="{{item.id}}">
        <image mode="aspectFill" src="{{item.icon}}"></image>
        <text>{{ item.desc }}</text>
    </view>
</view>