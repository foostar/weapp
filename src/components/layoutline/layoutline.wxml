<import src="../module/module.wxml" />

<view class="layoutline">
    <block wx:for="{{componentList}}">
        <template is="{{children[item.id].template}}" data="{{...children[item.id]}}" />
    </block>
</view>