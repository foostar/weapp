<import src="../module/module.wxml" />

<view class="layoutline">
    <view wx:if="{{showTitle}}" class="layout-title">{{title}}</view>
    <block wx:for="{{keys}}">
        <template is="{{children[item].template}}" data="{{...children[item]}}" />
    </block>
</view>