<!-- 登录后 -->
<view wx:if="{{isLogin}}">
    <view class="list-item" wx:if="{{isLogin}}">修改密码</view>
    <view class="list-item" wx:if="{{isLogin}}">回复提示通知</view>
    <view class="list-item" wx:if="{{isLogin}}">@提示通知</view>
    <view class="list-item" wx:if="{{isLogin}}">提示音</view>
    <view class="list-item" wx:if="{{isLogin}}">定位服务</view>
    <view class="list-item" wx:if="{{isLogin}}">位置信息</view>
</view>





<!-- 固定样式 -->
<view class="list-item">分享应用</view>
<view class="list-item">清空缓存</view>
<view class="list-item">新版本检测</view>
<view class="list-item">关于</view>
